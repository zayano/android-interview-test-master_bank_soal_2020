<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tokopedia.filter" >

    <application>
        <activity android:name=".view.ProductActivity"
            android:label="@string/filter">
        </activity>
        <activity android:name=".view.FilterActivity"
            android:label="@string/filter"/>
        <activity android:name=".view.FilterCaseActivity"
            android:label="@string/filter"/>
    </application>

</manifest>

